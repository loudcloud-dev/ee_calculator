<div class="container-fluid col-12">
  <%= render 'employee_search_form' %>

  <% if params[:employee_id].present? %>
    <div class="col-11 mx-auto">
      <%= render 'usage' %>
      <%= render 'item_breakdown' %>    
      <%= render 'filed_reimbursements' %>
    </div>
  <% end %>
</div>